"""
Парсер социальных сетей (заглушка).

Данный модуль является заглушкой для будущей реализации.
Поддерживаемые платформы будут определены в следующей итерации разработки.

Возможные направления:
- VK: vk_api / aiohttp + VK API
- Одноклассники: OK API
- LinkedIn: LinkedIn API (ограниченный доступ)
"""

from parsers.base import BaseParser, ParsedItem


class SocialParser(BaseParser):
    """Заглушка парсера социальных сетей.

    Всегда возвращает пустой список.
    Реализация запланирована в Этапе 7 (пост-релизные улучшения).
    """

    async def fetch(self) -> list[ParsedItem]:
        """Получить публикации из социальной сети (не реализовано).

        Returns:
            Всегда возвращает пустой список.
        """
        self.logger.warning(
            "SocialParser не реализован. Источник {} пропущен.", self.source_name
        )
        return []
